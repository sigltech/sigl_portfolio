$(window).scroll(function() {
    $('header').css({
      opacity: function() {
        var opacity = 0;
        //TODO:
        //Set opacity to a higer value whilst user scrolls
        return opacity;
      }
    });
  });
